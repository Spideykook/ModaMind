Drop a handful of JPG/PNG/WEBP images here (ideally including at least two
visually similar items) and run `python scripts/test_pipeline.py` from the
project root to verify the ResNet50 + FAISS pipeline in isolation.
